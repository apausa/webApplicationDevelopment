import { Status, StatusColor } from '@/types/lib';

export const STATUS: Status[] = ['PENDING', 'FULFILLED', 'REJECTED', null];
export const STATUS_COLOR: StatusColor[] = ['warning', 'success', 'danger', 'default'];
